language: python
python: "3.5"
script:
  - make CXX=clang++-5.0 tmp/strategy.o
  - make clean
  - make debug='-g --coverage' CXX=g++-7
  - bash <(curl -s https://codecov.io/bash)
  - which cppcheck && cppcheck --enable=all . || true

before_deploy: cp .gitignore_deploy .gitignore

deploy:
  provider: pages
  github-token: ${api_key}
  skip-cleanup: true
  on:
    branch: master

matrix:
  include:
    - os: linux
      addons:
        apt:
          update: true
          sources:
            - ubuntu-toolchain-r-test
            - llvm-toolchain-trusty-4.0
          packages:
            - clang++-5.0
            - g++-7
            - cppcheck
